server:
  port: 8761

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    healthcheck:
      enabled: true
    registerWithEureka: false # Eureka 서비스에 등록하지 않음
    fetchRegistry: true # Register 정보를 local 에 캐싱하지 않음
